
### `repo/notes.md`

```markdown
# Day 7 — Git Basics (Notes)

- `git init` creates a new repository in this folder.
- `git status` shows changes in the working directory and staging area.
- `git add` moves changes into the staging area.
- `git commit` records a snapshot in the local repository.
- `git log --oneline` shows a short commit history.
- `git restore` and `git reset` help undo mistakes.
```
